# Case 04 — Hayes Orbital Station

Current curriculum version: **0.1**
Status: **DRAFT · OWNER_REVIEW_NOT_STARTED**

This unreleased native curriculum-editor package implements Campaign 1 Case 04 for owner review. Its canonical development source is `source/case-package.json` plus `content.html`, `presentation.css`, and `task-registry.js` in `source/`.

Roles and page counts:

- Student: 4
- Teacher: 7
- Answer Key: 4
- Accessible: 6

Grayscale is an independent presentation toggle for every role and does not alter these counts or role filenames. Editable copies, role worksheets, and print documents are generated temporarily on demand and are not stored.

No release-history record exists while the case remains `DRAFT`. Create `history/release-vX.json` only after the owner advances the case through `VALIDATION_BUILD` and `OWNER_GATE_OPEN` to `APPROVED_STABLE`.

Owner review should verify the mission wording, reactor-specific science qualification, eight-task sequence, page fit, accessibility, canonical CER, and engineering-control decision before changing lifecycle status.
